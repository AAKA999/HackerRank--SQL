--MS SQL SERVER
SELECT NAME+'('+UPPER(SUBSTRING(OCCUPATION,1,1))+')'
FROM OCCUPATIONS 
ORDER BY NAME;

SELECT CONCAT('There are total ',CAST(COUNT(OCCUPATION) AS VARCHAR),' ',LOWER(OCCUPATION),'s.') AS COUNT
FROM OCCUPATIONS
GROUP BY OCCUPATION
ORDER BY COUNT;